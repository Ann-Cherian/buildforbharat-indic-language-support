export type Laptop = {
  id: number;
  brand: string;
  name: string;
  processor: string;
  ram: string;
  storage: string;
  price: number;
};

export type Message = {
  id: number;
  message: string;
};
